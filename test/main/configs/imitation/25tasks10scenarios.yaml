data:
  data_dir: "data/25tasks10scenarios"
  train_file: "train.jld2"
  validation_file: "validation.jld2"
train:
  log_dir: "final_experiments"
  tag: "imitation_25tasks10scenarios"
  batchsize: 1
  metrics:
    train_and_validation:
      - "Loss"
      - "AllCosts"
    train:
    validation:
  optimizer:
    name: "ADAM"
    args:
  nb_epochs: 100
  save_every_n_epochs: 100
  log_every_n_epochs: 1
model:
  name: "FenchelYoungGLM"
  args:
    M: 5
    nb_features: 20
    ε: 0.1
    model_builder: "grb_model"
