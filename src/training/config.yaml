train:
  batchsize: 1
  save_every_n_epochs: 100
  metrics:
    test: ~
    train:
      - "ModelWeights"
    train_and_test:
      - "Loss"
      - "AverageCostGap"
      - "MaxCostGap"
      - "AveragePerturbedCostGap"
  tag: "learning_by_experience"
  optimizer:
    name: "ADAM"
    args: ~
  nb_epochs: 300
  log_dir: "logs"
  log_every_n_epochs: 1
model:
  name: "PerturbedGLM"
  args:
    M: 5
    nb_features: 20
    ε: 1000
    model_builder: "grb_model"
data:
  data_dir: "data/data50"
  test_file: "test.jld2"
  train_file: "train.jld2"
